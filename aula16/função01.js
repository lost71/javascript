function parinpar(n) {
    if (n % 2 === 0) {
        return "par";
    } else {
        return "ímpar";
    }
}

// Exemplos de uso:
console.log(parinpar(4)); // Saída: "par"
console.log(parinpar(7)); // Saída: "ímpar"
